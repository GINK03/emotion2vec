{"context": " More than 1 year has passed since last update.\u4f8b\u3048\u3070\u3053\u3093\u306a\u30e6\u30fc\u30c6\u30a3\u30ea\u30c6\u30a3\u30fb\u30e1\u30bd\u30c3\u30c9\u3092\u5b9a\u7fa9\u3057\u3066\n    static addAddTos(Class clazz) {\n        def hasMany = clazz.metaClass.properties.find { it.name == \"hasMany\" }\n        hasMany.getProperty(clazz).each { k, v ->\n            clazz.metaClass.\"addTo${k.capitalize()}\" = { delegate.\"${k}\"?delegate.\"${k}\".add(it):(delegate.\"${k}\"=[it]); delegate }\n        }\n    }\n\n\u3053\u3093\u306a\u98a8\u306b\u4f7f\u3063\u3066\u3084\u308c\u3070\n    def setup() {\n        ......\n        ...addAddTos(Foo)\n        ......\n    }\n\n\u3061\u3087\u3063\u3068\u306f\u6c17\u304c\u6674\u308c\u308b\u304b\u3082\n\u4f8b\u3048\u3070\u3053\u3093\u306a\u30e6\u30fc\u30c6\u30a3\u30ea\u30c6\u30a3\u30fb\u30e1\u30bd\u30c3\u30c9\u3092\u5b9a\u7fa9\u3057\u3066\n\n```groovy\n    static addAddTos(Class clazz) {\n        def hasMany = clazz.metaClass.properties.find { it.name == \"hasMany\" }\n        hasMany.getProperty(clazz).each { k, v ->\n            clazz.metaClass.\"addTo${k.capitalize()}\" = { delegate.\"${k}\"?delegate.\"${k}\".add(it):(delegate.\"${k}\"=[it]); delegate }\n        }\n    }\n```\n\n\u3053\u3093\u306a\u98a8\u306b\u4f7f\u3063\u3066\u3084\u308c\u3070\n\n```groovy\n    def setup() {\n        ......\n        ...addAddTos(Foo)\n        ......\n    }\n```\n\n\u3061\u3087\u3063\u3068\u306f\u6c17\u304c\u6674\u308c\u308b\u304b\u3082", "tags": ["Grails"]}