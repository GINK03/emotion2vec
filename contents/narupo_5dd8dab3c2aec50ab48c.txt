{"context": " More than 1 year has passed since last update.XPending(3)\u3092\u4f7f\u3063\u305f\u3089\u3044\u3051\u305f\u3002\nhttps://groups.google.com/forum/#!topic/fj.mail-lists.x-window/oLUySRxi67c\n// Main loop\nfor (;;) {\n    // Polling loop\n    while (XPending(display)) {\n        XNextEvent(display, &event);\n        switch (event.type) {\n            // Do something\n        }\n    }\n    // Render and anything\n}\n\nXPending(3)\u3092\u4f7f\u3063\u305f\u3089\u3044\u3051\u305f\u3002\nhttps://groups.google.com/forum/#!topic/fj.mail-lists.x-window/oLUySRxi67c\n\n```c:\n// Main loop\nfor (;;) {\n    // Polling loop\n    while (XPending(display)) {\n        XNextEvent(display, &event);\n        switch (event.type) {\n            // Do something\n        }\n    }\n    // Render and anything\n}\n```\n", "tags": ["C", "Linux", "X11", "Xlib", "X"]}