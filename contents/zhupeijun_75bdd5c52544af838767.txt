{"tags": ["iOS", "Build", "number"], "context": " More than 1 year has passed since last update.\nSelect the target you want to setup.\nGo to Build Phase, click + mark -> New Run Script Phase or from the menu Editor -> Add Build Phase -> Add New Script Build Phase.\nCopy this to the run script content.\n\nif [ \"Release\" = \"${CONFIGURATION}\" ]; then\n  printf -v buildNumber \"%X\" `date +%s`\n  /usr/libexec/PlistBuddy -c \"Set :CFBundleVersion $buildNumber\" \"$INFOPLIST_FILE\"\nfi\n\nThis will setup up the build number with current time hex string. You also can change this way with commit count or any other way.\n\nMove the run script order before Copy Bundle Resources. \n\n* Select the target you want to setup.\n* Go to Build Phase, click `+` mark -> New Run Script Phase or from the menu Editor -> Add Build Phase -> Add New Script Build Phase.\n* Copy this to the run script content.\n\n```sh\nif [ \"Release\" = \"${CONFIGURATION}\" ]; then\n  printf -v buildNumber \"%X\" `date +%s`\n  /usr/libexec/PlistBuddy -c \"Set :CFBundleVersion $buildNumber\" \"$INFOPLIST_FILE\"\nfi\n```\n\n*This will setup up the build number with current time hex string. You also can change this way with commit count or any other way.*\n\n* Move the run script order before **Copy Bundle Resources**. \n"}