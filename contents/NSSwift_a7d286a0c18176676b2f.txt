{"context": " More than 1 year has passed since last update.Objective-C\nNSAlert * alert = [[NSAlert alloc] init];\n\n[alert beginSheetModalForWindow: self.view.window\n              completionHandler: ^( NSModalResponse returnCode)\n{\n    if ( returnCode == NSAlertSecondButtonReturn )\n    {\n        // do something\n    }\n}];\n\nSwift\nlet alert = NSAlert()\n\nalert.beginSheetModalForWindow( view.window! )\n{ returnCode in\n\n    if ( returnCode == NSAlertSecondButtonReturn )\n    {\n        // do something\n    }\n}\n\n\nObjective-C\n\n```Objective-C\nNSAlert * alert = [[NSAlert alloc] init];\n\t\n[alert beginSheetModalForWindow: self.view.window\n              completionHandler: ^( NSModalResponse returnCode)\n{\n\tif ( returnCode == NSAlertSecondButtonReturn )\n\t{\n\t\t// do something\n\t}\n}];\n```\n\nSwift\n\n```Swift\nlet alert = NSAlert()\n\t\t\nalert.beginSheetModalForWindow( view.window! )\n{ returnCode in\n\n\tif ( returnCode == NSAlertSecondButtonReturn )\n\t{\n\t\t// do something\n\t}\n}\n```\n", "tags": ["ObjCToSwift"]}