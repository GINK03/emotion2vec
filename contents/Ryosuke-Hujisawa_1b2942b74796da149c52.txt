{"context": "\n\n\u3053\u306e\u30b3\u30fc\u30c9\u3067\u52d5\u304d\u307e\u3059(\u306a\u305c\u304b\u306f\u77e5\u3089\u306a\u3044)\n\n# Uncomment this line to define a global platform for your project\n# platform :ios, \u201910.0\u2019\n\ntarget 'countTap' do\n  # Comment this line if you're not using Swift and don't want to use dynamic frameworks\n  use_frameworks!\n\n  # Pods for countTap\n\n  target 'countTapTests' do\n    inherit! :search_paths\n    # Pods for testing\n  end\n\n  target 'countTapUITests' do\n    inherit! :search_paths\n    # Pods for testing\n  end\n\n\npod 'RealmSwift'\n\nend\n\n\n\npost_install do |installer|\n  installer.pods_project.targets.each do |target|\n    target.build_configurations.each do |config|\n      config.build_settings['SWIFT_VERSION'] = '2.3' # or '3.0'\n    end\n  end\nend\n\n\n\n\u3053\u3053\u304c\u5927\u5207\uff08\u306a\u305c\u304b\u306f\u77e5\u3089\u306a\u3044)\n\npost_install do |installer|\n  installer.pods_project.targets.each do |target|\n    target.build_configurations.each do |config|\n      config.build_settings['SWIFT_VERSION'] = '2.3' # or '3.0'\n    end\n  end\nend\n\n#\u3053\u306e\u30b3\u30fc\u30c9\u3067\u52d5\u304d\u307e\u3059(\u306a\u305c\u304b\u306f\u77e5\u3089\u306a\u3044)\n\n\n\n\n```\n\n# Uncomment this line to define a global platform for your project\n# platform :ios, \u201910.0\u2019\n\ntarget 'countTap' do\n  # Comment this line if you're not using Swift and don't want to use dynamic frameworks\n  use_frameworks!\n\n  # Pods for countTap\n\n  target 'countTapTests' do\n    inherit! :search_paths\n    # Pods for testing\n  end\n\n  target 'countTapUITests' do\n    inherit! :search_paths\n    # Pods for testing\n  end\n  \n  \npod 'RealmSwift'\n\nend\n\n\n\npost_install do |installer|\n  installer.pods_project.targets.each do |target|\n    target.build_configurations.each do |config|\n      config.build_settings['SWIFT_VERSION'] = '2.3' # or '3.0'\n    end\n  end\nend\n\n```\n\n#\u3053\u3053\u304c\u5927\u5207\uff08\u306a\u305c\u304b\u306f\u77e5\u3089\u306a\u3044)\n\n```\n\npost_install do |installer|\n  installer.pods_project.targets.each do |target|\n    target.build_configurations.each do |config|\n      config.build_settings['SWIFT_VERSION'] = '2.3' # or '3.0'\n    end\n  end\nend\n```\n", "tags": ["RealmSwift", "Realm", "Xcode", "Xcode8"]}