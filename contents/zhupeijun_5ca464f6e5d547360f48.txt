{"context": " More than 1 year has passed since last update.If you want to send local notifications for the user. You can use the UILocalNotification. Just like this.\nvar notification = UILocalNotification()\nnotification.fireDate = NSDate(timeIntervalSinceNow: 10)\nnotification.timeZone = NSTimeZone.localTimeZone()\nnotification.alertBody = \"This is a notification!\"\nnotification.alertAction = \"Open\"\nnotification.soundName = UILocalNotificationDefaultSoundName\nUIApplication.sharedApplication().scheduleLocalNotification(notification)\n\nBut this is not done. May be you will get some error message in the console like this:\nAttempting to schedule a local notification ..... with an alert but haven't received permission from the user to display alerts\n\nFor this, you should ask the user to permit your app to send notifications when the app lunch in the didFinishLaunchingWithOptions from the AppDelegate file.\nfunc application(application: UIApplication, didFinishLaunchingWithOptions launchOptions: [NSObject: AnyObject]?) -> Bool {\n    if(UIApplication.instancesRespondToSelector(Selector(\"registerUserNotificationSettings:\"))) {\n        application.registerUserNotificationSettings( UIUserNotificationSettings(forTypes: UIUserNotificationType.Sound | UIUserNotificationType.Alert | UIUserNotificationType.Badge, categories: nil))\n    }\n    return true\n}\n\nTip: The notification only works when the app is in the background.\nIf you want to send local notifications for the user. You can use the `UILocalNotification`. Just like this.\n\n```swift\nvar notification = UILocalNotification()\nnotification.fireDate = NSDate(timeIntervalSinceNow: 10)\nnotification.timeZone = NSTimeZone.localTimeZone()\nnotification.alertBody = \"This is a notification!\"\nnotification.alertAction = \"Open\"\nnotification.soundName = UILocalNotificationDefaultSoundName\nUIApplication.sharedApplication().scheduleLocalNotification(notification)\n```\n\nBut this is not done. May be you will get some error message in the console like this:\n\n```\nAttempting to schedule a local notification ..... with an alert but haven't received permission from the user to display alerts\n```\n\nFor this, you should ask the user to permit your app to send notifications when the app lunch in the `didFinishLaunchingWithOptions` from the `AppDelegate` file.\n\n```swift\nfunc application(application: UIApplication, didFinishLaunchingWithOptions launchOptions: [NSObject: AnyObject]?) -> Bool {\n    if(UIApplication.instancesRespondToSelector(Selector(\"registerUserNotificationSettings:\"))) {\n        application.registerUserNotificationSettings( UIUserNotificationSettings(forTypes: UIUserNotificationType.Sound | UIUserNotificationType.Alert | UIUserNotificationType.Badge, categories: nil))\n    }\n    return true\n}\n```\n\n*Tip: The notification only works when the app is in the background.*\n", "tags": ["iOS", "Swift", "UILocalNotification", "Notification"]}