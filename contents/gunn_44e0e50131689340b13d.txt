{"context": " More than 1 year has passed since last update.subversion\u304c\u6b32\u3057\u304f\u306a\u3063\u305f\u306e\u3067\u30e1\u30e2\u3002\n$ sudo yum install subversion\n$ sudo yum install mod_dav_svn\n$ svn --version --quiet\n1.6.11\n$ mkdir -p /path/to/repos\n$ sudo svnadmin create /path/to/repos/sample\n$ echo \"export SVN_EDITOR=vim\" >> ~/.bashrc\n$ . .bashrc\n$ svn mkdir file:///path/to/repos/sample/trunk\n$ sudo vim /etc/httpd/conf.d/subversion.conf\n<Location /repos>\n    DAV svn\n    SVNPath /path/to/repos\n    AuthType Basic\n    AuthName \"Authorization Realm\"\n    AuthUserFile /data/repos/.htpasswd\n    Require valid-user\n</Location>\n$ sudo htpasswd -c /path/to/repos/.htpasswd USERNAME\n$ sudo /etc/rc.d/init.d/httpd restart\n\nsubversion\u304c\u6b32\u3057\u304f\u306a\u3063\u305f\u306e\u3067\u30e1\u30e2\u3002\n\n```\n$ sudo yum install subversion\n$ sudo yum install mod_dav_svn\n$ svn --version --quiet\n1.6.11\n$ mkdir -p /path/to/repos\n$ sudo svnadmin create /path/to/repos/sample\n$ echo \"export SVN_EDITOR=vim\" >> ~/.bashrc\n$ . .bashrc\n$ svn mkdir file:///path/to/repos/sample/trunk\n$ sudo vim /etc/httpd/conf.d/subversion.conf\n<Location /repos>\n    DAV svn\n    SVNPath /path/to/repos\n    AuthType Basic\n    AuthName \"Authorization Realm\"\n    AuthUserFile /data/repos/.htpasswd\n    Require valid-user\n</Location>\n$ sudo htpasswd -c /path/to/repos/.htpasswd USERNAME\n$ sudo /etc/rc.d/init.d/httpd restart\n", "tags": ["Linux", "Subversion"]}