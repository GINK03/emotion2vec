{"tags": ["JavaScript", "slice", "trim", "string"], "context": " More than 1 year has passed since last update.var query = 'a500b';\nvar trimed = query.slice(1, -1); //=> '500';\n\n\n```javascript\nvar query = 'a500b';\nvar trimed = query.slice(1, -1); //=> '500';\n```"}