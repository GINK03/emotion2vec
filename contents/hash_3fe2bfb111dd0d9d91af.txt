{"context": " More than 1 year has passed since last update.$ sudo vim /etc/init.d/monit\n\n/etc/init.d/monit\n  start() {\n          echo -n $\"Starting $prog: \"\n          daemon $prog\n          RETVAL=$?\n          echo\n          [ $RETVAL -eq 0 ] && touch /var/lock/subsys/$prog\n+         PID=`cat /var/run/monit.pid`\n+         echo -17 > /proc/$PID/oom_adj\n          return $RETVAL\n  }\n\n\n\u30d7\u30ed\u30bb\u30b9\u3092\u518d\u8d77\u52d5\u3057\u3001\u53cd\u6620\u3055\u308c\u3066\u3044\u308b\u3053\u3068\u3092\u78ba\u8a8d\n$ sudo systemctl --system daemon-reload\n$ sudo /etc/init.d/monit restart\nRestarting monit (via systemctl):                          [  OK  ]\n\n$ cat /proc/`cat /var/run/monit.pid`/oom_adj\n-17\n\n`$ sudo vim /etc/init.d/monit`\n\n~~~~~~diff:/etc/init.d/monit\n  start() {\n          echo -n $\"Starting $prog: \"\n          daemon $prog\n          RETVAL=$?\n          echo\n          [ $RETVAL -eq 0 ] && touch /var/lock/subsys/$prog\n+         PID=`cat /var/run/monit.pid`\n+         echo -17 > /proc/$PID/oom_adj\n          return $RETVAL\n  }\n~~~~~~\n\n\u30d7\u30ed\u30bb\u30b9\u3092\u518d\u8d77\u52d5\u3057\u3001\u53cd\u6620\u3055\u308c\u3066\u3044\u308b\u3053\u3068\u3092\u78ba\u8a8d\n\n~~~~~~bash\n$ sudo systemctl --system daemon-reload\n$ sudo /etc/init.d/monit restart\nRestarting monit (via systemctl):                          [  OK  ]\n\n$ cat /proc/`cat /var/run/monit.pid`/oom_adj\n-17\n~~~~~~\n\n", "tags": ["Linux", "fedora"]}