{"context": " More than 1 year has passed since last update.Public Function setArraytoDic(ByRef strArray As Variant) As Dictionary\n    Dim i As Long\n    Set setArraytoDic = CreateObject(\"Scripting.Dictionary\")\n    For i = 0 To UBound(strArray)\n        setArraytoDic.Add strArray(i), i + 1\n    Next i\n\nEnd Function\n\n```\nPublic Function setArraytoDic(ByRef strArray As Variant) As Dictionary\n    Dim i As Long\n    Set setArraytoDic = CreateObject(\"Scripting.Dictionary\")\n    For i = 0 To UBound(strArray)\n        setArraytoDic.Add strArray(i), i + 1\n    Next i\n    \nEnd Function\n```\n", "tags": ["VBA"]}