{"context": " More than 1 year has passed since last update.\n\nDiff\n# Rakefile\n+  targets.each do |target|\n+    Dir.glob(\"./spec/*/*_spec.rb\").each do |spec|\n+      original_target = target == \"_default\" ? target[1..-1] : target\n+      desc \"Run serverspec tests to #{original_target} (#{spec.split('/').last})\"\n+      RSpec::Core::RakeTask.new(spec.to_sym) do |t|\n+        ENV['TARGET_HOST'] = original_target\n+        t.pattern = \"spec/#{original_target}/#{spec.split('/').last}\"\n+      end\n+    end\n+  end\n\n\nCheck\nbundle exec rake -T\n\n# Diff\n\n```\n# Rakefile\n+  targets.each do |target|\n+    Dir.glob(\"./spec/*/*_spec.rb\").each do |spec|\n+      original_target = target == \"_default\" ? target[1..-1] : target\n+      desc \"Run serverspec tests to #{original_target} (#{spec.split('/').last})\"\n+      RSpec::Core::RakeTask.new(spec.to_sym) do |t|\n+        ENV['TARGET_HOST'] = original_target\n+        t.pattern = \"spec/#{original_target}/#{spec.split('/').last}\"\n+      end\n+    end\n+  end\n```\n\n\n# Check\n```\nbundle exec rake -T\n```\n", "tags": ["serverspec"]}