{"tags": ["JavaScript", "dom", "XML"], "context": " More than 1 year has passed since last update.DOM \u306e object \u3092 serialize \u3059\u308b\u306b\u306f\nfunciton serializeToString(node) {\n    try {\n        // for Firefox, Chrome, Safari, Opera\n        return (new XMLSerializer()).serializeToString(node);\n    } catch (e) {\n        try {\n            // for IE\n            return node.xml;\n        } catch (e2) {\n            alert(\"I can not serialize DOM node!\");\n        }\n    }\n    return false;\n}\n\n\nDOM \u306e object \u3092 serialize \u3059\u308b\u306b\u306f\n\n~~~JavaScript\nfunciton serializeToString(node) {\n    try {\n        // for Firefox, Chrome, Safari, Opera\n        return (new XMLSerializer()).serializeToString(node);\n    } catch (e) {\n        try {\n            // for IE\n            return node.xml;\n        } catch (e2) {\n            alert(\"I can not serialize DOM node!\");\n        }\n    }\n    return false;\n}\n~~~\n"}