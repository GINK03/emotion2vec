{"tags": ["regex", "C++"], "context": " More than 1 year has passed since last update.In c++11, we can use the regular expression to do something like matching.\nHere is some code about how to use the regex in c++11.\n#include <iostream>\n#include <regex>\n\nusing namespace std;\n\nint main() {\n    try {\n        string input = \"<file>abc.cpp</file>\";\n        regex re(\"<file>[\\\\w\\\\d]+\\\\.cpp</file>\");\n        smatch sm;\n        if (regex_match(input, sm, re)) {\n            cout << \"Matched\" << endl;\n            for (auto s : sm) {\n                cout << \"[\" << s << \"]\" << endl;\n            }\n        }\n    } catch (const regex_error& err) {\n        cout << err.what() << endl;\n    }\n    return 0;\n}\n\nIn c++11, we can use the regular expression to do something like matching.\n\nHere is some code about how to use the regex in c++11.\n\n```cpp\n#include <iostream>\n#include <regex>\n\nusing namespace std;\n\nint main() {\n    try {\n        string input = \"<file>abc.cpp</file>\";\n        regex re(\"<file>[\\\\w\\\\d]+\\\\.cpp</file>\");\n        smatch sm;\n        if (regex_match(input, sm, re)) {\n            cout << \"Matched\" << endl;\n            for (auto s : sm) {\n                cout << \"[\" << s << \"]\" << endl;\n            }\n        }\n    } catch (const regex_error& err) {\n        cout << err.what() << endl;\n    }\n    return 0;\n}\n```\n"}