{"context": " More than 1 year has passed since last update.If you want to generate a user with different uuid or email address, try to do this way.\nFactoryGirl.define do\n  factory :user do\n    uuid                  { SecureRandom.uuid }\n    sequence(:email)      { |n| \"test#{n}@abc.com\" }\n    password              \"password\"\n    password_confirmation \"password\"\n  end\nend\n\nPOINT:\nThe SecureRandom.uuid will generate same uuid if you do something like this.\nuuid  SecureRandom.uuid\n\nIf you want to generate a user with different uuid or email address, try to do this way.\n\n```rb\nFactoryGirl.define do\n  factory :user do\n    uuid                  { SecureRandom.uuid }\n    sequence(:email)      { |n| \"test#{n}@abc.com\" }\n    password              \"password\"\n    password_confirmation \"password\"\n  end\nend\n```\n\n**POINT:**\nThe `SecureRandom.uuid` will generate same uuid if you do something like this.\n\n```rb\nuuid  SecureRandom.uuid\n```\n", "tags": ["Rails", "FactoryGirl", "Random"]}