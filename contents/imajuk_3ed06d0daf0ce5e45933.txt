{"tags": ["ActionScript3", "Away3D4.1"], "context": " More than 1 year has passed since last update.const geom     : PrimitiveBase   = new PlaneGeometry(100, 100, 10, 10),\n      vertices : Vector.<Number> = ISubGeometry(geom.subGeometries[0]).vertexData;\n\nfor (var i : int = 0, l : int = vertices.length; i < l; )\n{\n    const \n        // position\n        vx : Number = vertices[i++],\n        vy : Number = vertices[i++],\n        vz : Number = vertices[i++],\n        // normals\n        nx : Number = vertices[i++],\n        ny : Number = vertices[i++],\n        nz : Number = vertices[i++],\n        // tangents\n        tx : Number = vertices[i++],\n        ty : Number = vertices[i++],\n        tz : Number = vertices[i++],\n        // primary uv\n        pu : Number = vertices[i++],\n        pv : Number = vertices[i++],\n        // secondary uv \n        sv : Number = vertices[i++],\n        sv : Number = vertices[i++];\n}\n\n\n```as3\nconst geom     : PrimitiveBase   = new PlaneGeometry(100, 100, 10, 10),\n      vertices : Vector.<Number> = ISubGeometry(geom.subGeometries[0]).vertexData;\n       \nfor (var i : int = 0, l : int = vertices.length; i < l; )\n{\n    const \n        // position\n        vx : Number = vertices[i++],\n        vy : Number = vertices[i++],\n        vz : Number = vertices[i++],\n        // normals\n        nx : Number = vertices[i++],\n        ny : Number = vertices[i++],\n        nz : Number = vertices[i++],\n        // tangents\n        tx : Number = vertices[i++],\n        ty : Number = vertices[i++],\n        tz : Number = vertices[i++],\n        // primary uv\n        pu : Number = vertices[i++],\n        pv : Number = vertices[i++],\n        // secondary uv \n        sv : Number = vertices[i++],\n        sv : Number = vertices[i++];\n}\n```"}