{"context": " More than 1 year has passed since last update.Clojure\u3001\u306f\u3058\u3081\u307e\u3057\u305f\u3002\n(defn random-string [n]\n  (let [random (java.util.Random. n)]\n    (clojure.string/join\n      (map #(char (+ % (int \\`)))\n           (take-while #(not= % 0) (repeatedly #(.nextInt random 27)))))))\n\n(println\n  (clojure.string/join \" \" (map random-string [-229985452 -147909649])))\n\nWhy does this code print \u201chello world\u201d? - stackoverflow\n\nClojure\u3001\u306f\u3058\u3081\u307e\u3057\u305f\u3002\n\n```clj\n(defn random-string [n]\n  (let [random (java.util.Random. n)]\n    (clojure.string/join\n      (map #(char (+ % (int \\`)))\n           (take-while #(not= % 0) (repeatedly #(.nextInt random 27)))))))\n\n(println\n  (clojure.string/join \" \" (map random-string [-229985452 -147909649])))\n```\n\n[Why does this code print \u201chello world\u201d? - stackoverflow](http://stackoverflow.com/questions/15182496/why-does-this-code-print-hello-world)", "tags": ["Clojure"]}