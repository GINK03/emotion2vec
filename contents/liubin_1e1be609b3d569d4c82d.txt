{"context": " More than 1 year has passed since last update.This strategy keep to bellow policies:\n\nminimum privilege:only select and lock tables privilege is needed.\nNo password, no network\n\n\nCreate user\n> grant select,lock tables ON  db.* TO 'backup'@'localhost';\n\nWithout the lock tables privilege, you will get error:\nmysqldump: Got error: 1044: \"Access denied for user 'backup'@'localhost' to database 'db'\" when using LOCK TABLES\n\n\nDump database via mysql.sock\n\nmysqldump -ubackup -S/var/lib/mysql/mysql.sock\n\nThis strategy keep to bellow policies:\n\n1. minimum privilege:only `select` and `lock tables` privilege is needed.\n2. No password, no network\n\n# Create user\n\n```\n> grant select,lock tables ON  db.* TO 'backup'@'localhost';\n```\n\nWithout the `lock tables` privilege, you will get error:\n\n```\nmysqldump: Got error: 1044: \"Access denied for user 'backup'@'localhost' to database 'db'\" when using LOCK TABLES\n```\n\n# Dump database via `mysql.sock`\n\n```\nmysqldump -ubackup -S/var/lib/mysql/mysql.sock\n```\n\n", "tags": ["MySQL", "mysqldump"]}