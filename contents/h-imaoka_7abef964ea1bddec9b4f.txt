{"context": "\n\nRosetta stone\nhttps://certsimple.com/rosetta-stone\nhttp://bhami.com/rosetta.html\n\nfrom yum/rpm to apt/dpkg\n\n1st, apt-get update. It DO NOT update packege, just only update apt-cache\n\n\nyum update : apt-get upgrade\nupgrade apply miner version updates.\n\nyum update <hoge> : apt-get install <hoge>\n\n\nyum search <hoge> : apt-cache search <hoge>\n\n\nrpm -qa : dpkg -l\n\n\nrpm -qi <hoge> : dpkg -s <hoge> or dpkg -p <hoge\n\n\nrpm -qf <file> : dpkg -S <file>\n\n\nrpm -ql <hoge> : dpkg -L <hoge>\n\n\n\nWhere is changelog ?\napt-get changelog <hoge>. but this command read latest ver's changelog on remote repository. Moreover, unfortunately some URL returns 404... \nWe can read changelog zless /usr/share/doc/<hoge>/changelog.Debian.gz. \n\ninit.d or systemd ?\nubuntu 14.04 = init.d\nubuntu 16.04 = systemd\nDebian 7 = init.d\nDebian 8 = systemd\n\n# Rosetta stone\nhttps://certsimple.com/rosetta-stone\nhttp://bhami.com/rosetta.html\n\n# from `yum/rpm` to `apt/dpkg`\n1st, `apt-get update`. It ___DO NOT___ update packege, just only update `apt-cache`\n\n- `yum update` : `apt-get upgrade`\n`upgrade` apply miner version updates.\n- `yum update <hoge>` : `apt-get install <hoge>`\n- `yum search <hoge>` : `apt-cache search <hoge>`\n- `rpm -qa` : `dpkg -l`\n- `rpm -qi <hoge>` : `dpkg -s <hoge>` or `dpkg -p <hoge`\n- `rpm -qf <file>` : `dpkg -S <file>`\n- `rpm -ql <hoge>` : `dpkg -L <hoge>`\n\n## Where is changelog ?\n`apt-get changelog <hoge>`. but this command read latest ver's changelog on remote repository. Moreover, unfortunately some URL returns 404... \nWe can read changelog `zless /usr/share/doc/<hoge>/changelog.Debian.gz`. \n\n# `init.d` or `systemd` ?\nubuntu 14.04 = init.d\nubuntu 16.04 = systemd\nDebian 7 = init.d\nDebian 8 = systemd\n", "tags": ["Ubuntu", "Linux", "Debian"]}