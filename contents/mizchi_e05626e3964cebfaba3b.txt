{"context": " More than 1 year has passed since last update.mizchi/gulp-reiny \u3068mizchi/reinify \u3092\u4f5c\u3063\u305f\u6642\u306e\u30e1\u30e2\u3002\u30b3\u30f3\u30d1\u30a4\u30e9\u3092\u4f5c\u3063\u305f\u3002\u6587\u5b57\u5217\u3092\u52a0\u5de5\u3057\u3066\u8fd4\u3057\u305f\u3044\u3002\u305d\u308c\u3060\u3051\u3002\ncompile = (foo) -> foo \u307f\u305f\u3044\u306a\u95a2\u6570\u304c\u3042\u308b\u3068\u3057\u3066\u3001\u305d\u306e\u7d50\u679c\u3092\u30e9\u30c3\u30d7\u3057\u305f\u3044\u3060\u3051\u306e\u3082\u306e\u3092\u4f5c\u308b\u3068\u304d\u3001\u3069\u3046\u306a\u308b\u304b\u3002\n\ngulp\nthrough = require 'through2'\nreplaceExt = require 'replace-ext'\ncompile = (text) -> text\n\nmodule.exports = (opt) ->\n  stream = through.obj (file, enc, callback) ->\n    if file.isNull()\n      @push(file)\n      return callback()\n    else if file.isBuffer()\n      file.contents = new Buffer compile(file.contents.toString())\n      file.path = replaceExt(file.path, '.js')\n      @push(file)\n      return callback()\n    else if file.isStream()\n      throw new Error('Streams are not supported')\n    return callback()\n  return stream\n\n\nbrowserify\nthrough = require('through')\ncompile = (text) -> text\n\nmodule.exports = (file) ->\n  inputString = ''\n  write = (buf) -> inputString += buf\n  end = ->\n    @queue(compile(inputString))\n    this.queue(null)\n  return through(write, end)\n\n\n[mizchi/gulp-reiny](https://github.com/mizchi/gulp-reiny \"mizchi/gulp-reiny\") \u3068[mizchi/reinify](https://github.com/mizchi/reinify \"mizchi/reinify\") \u3092\u4f5c\u3063\u305f\u6642\u306e\u30e1\u30e2\u3002\u30b3\u30f3\u30d1\u30a4\u30e9\u3092\u4f5c\u3063\u305f\u3002\u6587\u5b57\u5217\u3092\u52a0\u5de5\u3057\u3066\u8fd4\u3057\u305f\u3044\u3002\u305d\u308c\u3060\u3051\u3002\n\n`compile = (foo) -> foo` \u307f\u305f\u3044\u306a\u95a2\u6570\u304c\u3042\u308b\u3068\u3057\u3066\u3001\u305d\u306e\u7d50\u679c\u3092\u30e9\u30c3\u30d7\u3057\u305f\u3044\u3060\u3051\u306e\u3082\u306e\u3092\u4f5c\u308b\u3068\u304d\u3001\u3069\u3046\u306a\u308b\u304b\u3002\n\n## gulp\n\n```coffee\nthrough = require 'through2'\nreplaceExt = require 'replace-ext'\ncompile = (text) -> text\n\nmodule.exports = (opt) ->\n  stream = through.obj (file, enc, callback) ->\n    if file.isNull()\n      @push(file)\n      return callback()\n    else if file.isBuffer()\n      file.contents = new Buffer compile(file.contents.toString())\n      file.path = replaceExt(file.path, '.js')\n      @push(file)\n      return callback()\n    else if file.isStream()\n      throw new Error('Streams are not supported')\n    return callback()\n  return stream\n```\n\n## browserify\n\n```coffee\nthrough = require('through')\ncompile = (text) -> text\n\nmodule.exports = (file) ->\n  inputString = ''\n  write = (buf) -> inputString += buf\n  end = ->\n    @queue(compile(inputString))\n    this.queue(null)\n  return through(write, end)\n```\n", "tags": ["gulp", "browserify"]}