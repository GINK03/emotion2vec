{"context": " More than 1 year has passed since last update.$ cd git/gollum\n$ cp my-wiki-from-bitbucket/*.md . \n$ git add -A .\n$ git commit -m \"Added a lot of .md contents\"\n$ rbenv exec bundle exec gollum\n\n```\n$ cd git/gollum\n$ cp my-wiki-from-bitbucket/*.md . \n$ git add -A .\n$ git commit -m \"Added a lot of .md contents\"\n$ rbenv exec bundle exec gollum\n```\n", "tags": ["gollum"]}