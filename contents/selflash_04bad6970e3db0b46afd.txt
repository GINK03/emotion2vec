{"context": " More than 1 year has passed since last update.vector<flash::DisplayObject*> children;\n\nflash::DisplayObject* DisplayObjectContainer::getChildAt(const int& index) {\n    if(index < 0 || index > children.size() - 1) return NULL;\n    return children[index];\n}\n\n```cpp\nvector<flash::DisplayObject*> children;\n\nflash::DisplayObject* DisplayObjectContainer::getChildAt(const int& index) {\n    if(index < 0 || index > children.size() - 1) return NULL;\n    return children[index];\n}\n```", "tags": ["C++", "openFrameworks"]}