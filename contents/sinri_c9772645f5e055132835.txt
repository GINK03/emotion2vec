{"context": "When use apt-get update after add an https source to source list of apt on debian, an error came out as,\nE: The method driver /usr/lib/apt/methods/http could not be found.\n\nIt seems to be caused by the CA list on local is out of date, or simply not exists.\nIf apt still works well, use \napt-get install apt-transport-https ca-certificates\n\nto install the CA.\nIf your apt-get is too broken to do this, you can download the package directly from \n\nhttps://packages.debian.org/jessie/apt-transport-https \n\nand install it with dpkg -i. Of course, you may need to confirm the version of debian.\nWhen use `apt-get update` after add an https source to source list of apt on debian, an error came out as,\n\n    E: The method driver /usr/lib/apt/methods/http could not be found.\n\nIt seems to be caused by the CA list on local is out of date, or simply not exists.\n\nIf apt still works well, use \n\n    apt-get install apt-transport-https ca-certificates\n\nto install the CA.\n\nIf your apt-get is too broken to do this, you can download the package directly from \n\n> https://packages.debian.org/jessie/apt-transport-https \n\nand install it with dpkg -i. Of course, you may need to confirm the version of debian.\n", "tags": ["apt-get", "Debian"]}