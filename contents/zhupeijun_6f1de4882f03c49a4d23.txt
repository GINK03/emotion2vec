{"context": " More than 1 year has passed since last update.If you want to only push the current branch without the branch name, you can use this command to setup your config file.\ngit config --global push.default current\n\nThis will add the following things to your .gitconfig file.\n[push]\n  default = current\n\nIf you want to only push the current branch without the branch name, you can use this command to setup your config file.\n\n```\ngit config --global push.default current\n```\n\nThis will add the following things to your `.gitconfig` file.\n\n```\n[push]\n  default = current\n```\n", "tags": ["Git", "Push", "branch"]}