{"context": " More than 1 year has passed since last update.$root = XCube_Root::getSingleton();\n\nif ( $root->mContext->mUser->mIdentity->isAuthenticated() ) {\n    // \u8a8d\u8a3c\u6e08\u307f\n} else {\n    // \u672a\u8a8d\u8a3c\n} \n\n\n```php\n$root = XCube_Root::getSingleton();\n\nif ( $root->mContext->mUser->mIdentity->isAuthenticated() ) {\n\t// \u8a8d\u8a3c\u6e08\u307f\n} else {\n\t// \u672a\u8a8d\u8a3c\n} \n```", "tags": ["XOOPS", "PHP"]}