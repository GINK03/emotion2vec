{"tags": ["rust0.11", "unicode"], "context": " More than 1 year has passed since last update.\nglyph_len.rs\nfn glyph_len(string : &str) -> uint {\n\n    string.graphemes(true).count()\n\n}\n\ntrait GlyphLen {\n\n    fn glyph_len(&self) -> uint;\n\n}\n\nimpl<'a> GlyphLen for &'a str {\n\n    fn glyph_len(&self) -> uint {\n\n        glyph_len(*self)\n\n    }\n\n}\n\nfn main() {\n\n    let a = \"\\u304b\\u3099\"; // \u304c\n    println!(\"{} : {} : {}\",a,\n                            a.glyph_len(), // \u6587\u5b57\u5217\u306e\u8868\u793a\u4e0a\u306e\u9577\u3055 -> 1\n                            a.char_len() // UTF-16\u3067\u30a8\u30f3\u30b3\u30fc\u30c9\u3055\u308c\u305f\u6587\u5b57\u5217\u306e\u30d0\u30a4\u30c8\u6570(?) -> 2\n                            );\n\n}\n\n\n```rust:glyph_len.rs\nfn glyph_len(string : &str) -> uint {\n\n    string.graphemes(true).count()\n\n}\n\ntrait GlyphLen {\n\n    fn glyph_len(&self) -> uint;\n\n}\n\nimpl<'a> GlyphLen for &'a str {\n\n    fn glyph_len(&self) -> uint {\n\n        glyph_len(*self)\n\n    }\n\n}\n\nfn main() {\n\n    let a = \"\\u304b\\u3099\"; // \u304c\n    println!(\"{} : {} : {}\",a,\n                            a.glyph_len(), // \u6587\u5b57\u5217\u306e\u8868\u793a\u4e0a\u306e\u9577\u3055 -> 1\n                            a.char_len() // UTF-16\u3067\u30a8\u30f3\u30b3\u30fc\u30c9\u3055\u308c\u305f\u6587\u5b57\u5217\u306e\u30d0\u30a4\u30c8\u6570(?) -> 2\n                            );\n\n}\n```\n"}