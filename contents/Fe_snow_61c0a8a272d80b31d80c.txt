{"context": " More than 1 year has passed since last update.There are so many environment settings in Mac PCs.\ne.g. .zshrc, .vimrc, .gitconfig, .tmux.conf, etc\nSync these files with multiple Mac PCs with Dropbox.\nJust create symbolic links like this.\nln -s ~/Dropbox/env/.zshrc ~/.zshrc\n\nThere are so many environment settings in Mac PCs.\ne.g. `.zshrc`, `.vimrc`, `.gitconfig`, `.tmux.conf`, etc\n\nSync these files with multiple Mac PCs with [Dropbox](https://www.dropbox.com).\n\nJust create symbolic links like this.\n`ln -s ~/Dropbox/env/.zshrc ~/.zshrc`\n", "tags": ["OSX", "Dropbox", "Mac"]}