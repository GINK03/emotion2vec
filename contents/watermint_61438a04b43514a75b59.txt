{"context": " More than 1 year has passed since last update.\n\nKibana4 and requirements for instance which launched thorough Java API\nLaunch elasticsearch through Java API.\n\nOpen HTTP Port.\nEnable http Cross domain access for Kibana4.\n\nimport java.nio.file.Paths\n\nimport org.elasticsearch.client.Client\nimport org.elasticsearch.common.settings.ImmutableSettings\nimport org.elasticsearch.node.{Node, NodeBuilder}\n\nobject Main {\n  def main(args: Array[String]) {\n    val path = Paths.get(\"./elasticsearch\")\n    NodeBuilder\n      .nodeBuilder()\n      .clusterName(\"test\")\n      .local(false)\n      .settings(\n        ImmutableSettings\n          .settingsBuilder()\n          .put(\"path.home\", path)\n          .put(\"path.logs\", path.resolve(\"logs\"))\n          .put(\"http.enabled\", true)\n          .put(\"http.port\", 9200)\n          .put(\"http.cors.enabled\", true)\n          .put(\"http.cors.allow-origin\", \"/.*/\")\n      ).node().client()\n  }\n}\n\nSince Kibana4 using script plugin groovy. Latest elasticsearch contains groovy plugin itself, but not groovy runtime. And this dependency is not solved. We have to add groovy runtime explicitly like below.\nname := \"kibana4-and-elasticserach\"\n\nlibraryDependencies ++= Seq(\n  \"org.elasticsearch\" % \"elasticsearch\" % \"latest.release\",\n  \"org.codehaus.groovy\" % \"groovy-all\" % \"latest.release\"\n)\n\n# Kibana4 and requirements for instance which launched thorough Java API\n\nLaunch elasticsearch through Java API.\n\n* Open HTTP Port.\n* Enable http Cross domain access for Kibana4.\n\n```scala\nimport java.nio.file.Paths\n\nimport org.elasticsearch.client.Client\nimport org.elasticsearch.common.settings.ImmutableSettings\nimport org.elasticsearch.node.{Node, NodeBuilder}\n\nobject Main {\n  def main(args: Array[String]) {\n    val path = Paths.get(\"./elasticsearch\")\n    NodeBuilder\n      .nodeBuilder()\n      .clusterName(\"test\")\n      .local(false)\n      .settings(\n        ImmutableSettings\n          .settingsBuilder()\n          .put(\"path.home\", path)\n          .put(\"path.logs\", path.resolve(\"logs\"))\n          .put(\"http.enabled\", true)\n          .put(\"http.port\", 9200)\n          .put(\"http.cors.enabled\", true)\n          .put(\"http.cors.allow-origin\", \"/.*/\")\n      ).node().client()\n  }\n}\n```\n\nSince Kibana4 using script plugin groovy. Latest elasticsearch contains groovy plugin itself, but not groovy runtime. And this dependency is not solved. We have to add groovy runtime explicitly like below.\n\n```sbt\nname := \"kibana4-and-elasticserach\"\n\nlibraryDependencies ++= Seq(\n  \"org.elasticsearch\" % \"elasticsearch\" % \"latest.release\",\n  \"org.codehaus.groovy\" % \"groovy-all\" % \"latest.release\"\n)\n```\n", "tags": ["Elasticsearch", "Kibana4", "Scala", "Java"]}