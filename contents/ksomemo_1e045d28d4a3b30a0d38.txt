{"tags": ["memo"], "context": "\nskleran_estimated_params.py\n# Estimated parameters: http://scikit-learn.org/stable/tutorial/statistical_inference/settings.html\ndef estimated_params(estimator):\n    return [a for a in dir(estimator) if a.endswith(\"_\") and not a.startswith(\"_\")]\n\n\n```py3:skleran_estimated_params.py\n# Estimated parameters: http://scikit-learn.org/stable/tutorial/statistical_inference/settings.html\ndef estimated_params(estimator):\n    return [a for a in dir(estimator) if a.endswith(\"_\") and not a.startswith(\"_\")]\n```\n"}