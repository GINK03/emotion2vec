{"context": "\u5f8c\u65e5\u3001\u5c11\u3057\u3065\u3064\u307e\u3068\u3081\u308b\u305f\u3081\u306e\u30e1\u30e2\nSQL\u6587\n$this->db->last_query();\n\u7dcf\u4ef6\u6570\n$this->db->?\n    //\u30d8\u30c3\u30c0\u60c5\u5831\u8ffd\u52a0\n    foreach(['total','count','offset','debug'] as $entry){\n      switch ($entry){\n      case 'total':\n      //if(empty($$entry)){break;}          ==>\u6c4e\u7528\u7684\u306a\u66f8\u304d\u65b9\u3060\u3051\u3069\u308f\u304b\u3089\u306a\u304f\u306a\u308a\u305d\u3046\u306a\n      //$result['head'][$entry] = $$entry;\n        if(empty($total)){break;}\n        $result['head'][$entry] = $total;\n        break;\n      case 'count':\n        if(empty($limit)){break;}\n        $result['head'][$entry] = $limit;\n        break;\n      case 'offset':\n        if(empty($offset)){break;}\n        $result['head'][$entry] = $offset;\n        break;\n      case 'debug':\n        //$result['head'][$entry]['source']  = $sources;\n        $result['head'][$entry]['SQL']  = $this->db->last_query();\n        $result['head'][$entry]['args'] = json_encode($ary_args);\n        break;\n      }\n    }\n\n\n\u5f8c\u65e5\u3001\u5c11\u3057\u3065\u3064\u307e\u3068\u3081\u308b\u305f\u3081\u306e\u30e1\u30e2\n\n\nSQL\u6587\n```$this->db->last_query();```\n\n\u7dcf\u4ef6\u6570\n```$this->db->?```\n\n\n```php\n    //\u30d8\u30c3\u30c0\u60c5\u5831\u8ffd\u52a0\n    foreach(['total','count','offset','debug'] as $entry){\n      switch ($entry){\n      case 'total':\n      //if(empty($$entry)){break;}          ==>\u6c4e\u7528\u7684\u306a\u66f8\u304d\u65b9\u3060\u3051\u3069\u308f\u304b\u3089\u306a\u304f\u306a\u308a\u305d\u3046\u306a\n      //$result['head'][$entry] = $$entry;\n        if(empty($total)){break;}\n        $result['head'][$entry] = $total;\n        break;\n      case 'count':\n        if(empty($limit)){break;}\n        $result['head'][$entry] = $limit;\n        break;\n      case 'offset':\n        if(empty($offset)){break;}\n        $result['head'][$entry] = $offset;\n        break;\n      case 'debug':\n        //$result['head'][$entry]['source']  = $sources;\n        $result['head'][$entry]['SQL']  = $this->db->last_query();\n        $result['head'][$entry]['args'] = json_encode($ary_args);\n        break;\n      }\n    }\n```\n", "tags": ["CodeIgniter"]}