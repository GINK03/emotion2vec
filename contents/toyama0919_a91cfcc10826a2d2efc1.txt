{"context": " More than 1 year has passed since last update.  def defaultEncoding = 'UTF-8'\n  tasks.withType(AbstractCompile) each { it.options.encoding = defaultEncoding }\n\n  compileTestJava {\n      options.encoding = defaultEncoding\n  }\n\n\n\n```gradle\n  def defaultEncoding = 'UTF-8'\n  tasks.withType(AbstractCompile) each { it.options.encoding = defaultEncoding }\n\n  compileTestJava {\n      options.encoding = defaultEncoding\n  }\n```\n", "tags": ["gradle", "Java"]}